# 일본어 다독(多読) 앱 — 개발 명세서

작성일: 2026-07-28 · 용도: 클로드 코드 세션 참조 문서
(원본 `dokudo-app-CLAUDE.md`를 바탕으로, 사용 대상·배포 방식 변경을 반영해 갱신함)

---

## 0. 이 문서를 읽는 클로드에게 (작업 지침)

- 사용자는 IT 비전문가다. 전문 용어가 나오면 반드시 쉬운 말로 한 줄 풀이를 덧붙인다.
- 각 단계가 끝나면 "브라우저에서 이렇게 열어 확인하세요"까지 함께 안내한다.
- 일본어 지문을 만들 때 기존 저작물(드라마 대본, 노래 가사, 책 문장)을 가져오지 않는다. 전부 새로 창작한다.
- 2장의 설계 원칙과 충돌하는 기능은 제안하지 않는다 (예: 문법 강의 화면, 말하기 시험, 점수 압박 요소).
- 큰 구조 변경은 진행 전에 사용자에게 먼저 설명하고 확인받는다.
- 여러 직원이 쓰지만 **서로 데이터를 주고받는 기능은 만들지 않는다**. 각자 자기 기기에서 읽고, 진도도 각자 기기에만 저장된다. (진도를 서버에 모으는 기능은 요청이 있을 때만 재논의)
- **배포할 때마다 `index.html`의 `APP_VERSION` 숫자를 1 올린다.** 홈 화면 앱이 이 값으로 새 버전을 감지해 업데이트 안내를 띄운다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 목적 | 한국인 성인의 일본어 자연 습득 — 다독(문법 설명 없이 레벨별 지문을 많이 읽기) 방식 |
| 사용자 | 개발자 본인 + 같이 일하는 직원들. 각자 앱으로 공부하고, 정기적으로 모여 일본어로 대화하는 모임용 |
| 사용 형태 | 개인별 독립 사용 — 계정·서버 없음. 진도는 각자 브라우저에 저장 |
| 형태 | 단일 HTML 파일 하나(`index.html`)로 데스크탑 브라우저 + 모바일 겸용 |
| 배포 | GitHub Pages — **https://taehunyeoul-dot.github.io/dokudo/** (저장소: `taehunyeoul-dot/dokudo`, main 브랜치 루트). `index.html`을 수정해 커밋·푸시하면 1~2분 내 반영 |

> GitHub Pages: 깃허브(코드 보관 사이트)가 무료로 제공하는 웹사이트 호스팅. 파일을 올려두기만 하면 인터넷 주소로 접속할 수 있다. 무료 계정은 **공개 저장소**만 가능하지만, 콘텐츠가 전부 자체 창작이라 공개해도 문제없다.
> 정적 호스팅: 파일을 "보여주기만" 하고 저장은 못 하는 방식. 그래서 진도는 서버가 아니라 각자 기기(localStorage)에 저장한다.

---

## 2. 설계 원칙 — 크라센 '이해 가능한 입력' 이론 (모든 기능의 기준)

1. **언어는 메시지를 '이해'할 때 습득된다.** 문법 설명 기능은 넣지 않는다.
2. **i+1**: 현재 수준(i)보다 살짝 어려운 입력을 유지한다. 문장 단위 정밀 계산이 아니라 "대체로 이해되는 자료를 충분한 양으로"가 목표다.
3. **최적 이해도는 95~98%** — 한 페이지에 모르는 단어 1~2개 수준.
4. **재미가 난이도보다 우선한다** (정의적 여과: 지루하거나 압박이 있으면 이해해도 흡수가 막힌다).
5. **말하기를 강제하지 않는다** (침묵기: 말하기는 습득의 원인이 아니라 결과로 본다). 모임에서의 대화는 앱 밖에서 자연스럽게 이루어지는 것이며, 앱이 말하기를 시키지 않는다.
6. **같은 자료 반복이 새 자료 추가보다 우선한다.** 새 지문 10편보다 같은 지문 3회 읽기.

| 이해되는 비율 | 상태 | 조치 |
|---|---|---|
| 거의 100% | 편하지만 성장 없음 | 한 단계 위로 |
| 95~98% | 최적 구간 | 유지 |
| 90% 이하 | 사전 붙잡게 됨 | 한 단계 아래로 |

---

## 3. 콘텐츠 전략

| 역할 | 소스 | 비고 |
|---|---|---|
| 읽기 본줄기 | 클로드가 창작한 원본 지문 | 저작권 문제 없음. 레벨·단어 반복을 의도적으로 통제 가능 |
| 보조 예문 사전 (후순위·선택) | Tatoeba | 무료 예문 모음(CC BY 2.0 FR 라이선스). "이 단어의 실제 예문 더 보기" 용도로만 |

- Tatoeba를 본줄기로 쓰지 않는 이유: 낱개 문장이라 이야기 속 단어 반복이 없고, 난이도 표시가 없으며, 한국어 뜻이 없어 일→영→한 이중 번역(피벗 번역)을 거쳐야 해서 뜻이 어긋난다.
- 지문 창작 시 같은 핵심 단어가 한 이야기 안에서 3회 이상 자연스럽게 반복되도록 설계한다.
- 모임에서 대화 소재로 쓸 수 있도록, 일상 소재(아침, 식사, 주말, 친구 등) 중심으로 창작하면 좋다.
- 한국인 학습자 이점: 어순(SOV)과 조사 구조가 한국어와 거의 1:1이고 한자어를 공유한다. 히라가나만 떼면 이미 출발점(i)이 높다.

### 커리큘럼 — 완전 초보(일본어 경험 0) 기준

사용자(개발자 본인 포함)는 일본어를 전혀 모르는 상태에서 시작한다. 히라가나는 문법이 아니라 '책을 열 수 있는 열쇠'이므로, 글자 학습은 문법 설명 금지 원칙과 충돌하지 않는다.

| 단계 | 이름 | 하는 것 | 다음 단계로 가는 기준 |
|---|---|---|---|
| 0 | 글자 떼기 | 히라가나를 행별(あ행 5자씩)로 만나기 + 배운 글자만 쓰는 꼬마 단어 읽기 + 소리 듣기(TTS) | '섞어 읽기' 페이지를 더듬더듬 읽고 스스로 체크 |
| 1 | 히라가나 그림책 | 초단문 다독 | 탭 비율 2% 미만 (i+1 엔진 권유) |
| 2 | 기초 한자 + 후리가나 | 일상 서술문 다독 | 동일 |
| 3 | 짧은 이야기 | 과거형·연결형이 섞인 이야기 | — |

- 0단계도 '외우고 나서 읽기'가 아니라 **읽으면서 외우기**: 한 행을 배우면 곧바로 그 글자들로 된 단어를 뜻과 함께 읽는다. 시험·점수 없음, 자가 체크만 (압박 금지 원칙).
- **가타카나는 별도 단계를 두지 않는다.** 지문에서 만날 때마다 히라가나 후리가나(`r`)를 달아 자연스럽게 익힌다.
- 요음(きゃ)·촉음(っ)도 0단계에서 외우게 하지 않고, 지문에서 만날 때 탭 뜻과 소리로 익힌다.
- **모임 연결(북클럽 방식)**: 그 주의 책 2~3편을 정해 모임 전까지 각자 3회씩 읽는다 (반복 우선 원칙과 맞물림). 초기 모임은 말하기 강제 없이 같이 소리 내어 읽기, 좋았던 문장 공유 등 저압력 활동 위주 (침묵기 원칙).

### 레벨 체계

| 레벨 | 표기 | 내용 | 책등 색 |
|---|---|---|---|
| 0 | 히라가나 글자책 | 행별 글자 + 꼬마 단어 (46자 + 탁음) | `#8B5E34` (갈색) |
| 1 | 히라가나만 | 그림책 수준 단문 | `#6B7A5E` (초록) |
| 2 | 기초 한자 + 후리가나 | 일상 서술문 | `#2B4570` (남색) |
| 3 (확장안) | 동사 활용 확대 | 짧은 이야기 | 미정 |

---

## 4. 앱 구조 — 3층 + 엔진

```
콘텐츠(책) → 화면(리더) → 탭 기록(공책) → i+1 엔진 → 다음 책 추천
```

| 층 | 비유 | 담는 것 | 성격 |
|---|---|---|---|
| 콘텐츠 층 | 책 | 지문, 후리가나, 한국어 뜻 (JSON) | 무겁고 거의 안 바뀜 |
| 화면 층 | 읽는 기계 | 서가, 리더, 단어 탭 뜻 보기, 읽어주기 버튼 | 아무것도 기억하지 않음 |
| 기록 층 | 공책 | 완독 여부, 단어 탭 기록, 현재 레벨 (localStorage) | 가볍고 계속 바뀜. **사람마다 자기 기기에 따로 존재** |

> JSON: 데이터를 글자로 정리해 두는 약속된 표기법.
> localStorage: 브라우저가 기기 안에 마련해 둔 작은 메모장. 서버 없이도 껐다 켜도 유지된다.

- 화면 층과 콘텐츠 층을 분리해 두면, 나중에 디자인을 갈아엎어도 데이터는 건드리지 않는다.
- 기기 간에 옮겨야 하는 것은 **기록 층뿐**이다.

---

## 5. 데이터 구조

### 콘텐츠 (책 1권)

```json
{
  "id": "l1-cat",
  "level": 1,
  "title": "ねこの いちにち",
  "subtitle": "고양이의 하루",
  "sentences": [
    [
      {"t": "ねこ", "g": "고양이"},
      {"t": "が"},
      {"t": "います", "g": "있어요"},
      {"t": "。"}
    ]
  ]
}
```

- 토큰 규칙: `t` = 표기, `r` = 후리가나(레벨 2부터), `g` = 한국어 뜻.
- `g`가 없는 토큰(조사·문장부호)은 탭할 수 없다.
- 레벨 2 예: `{"t": "私", "r": "わたし", "g": "나"}` — 한자 위에 작은 발음 글자를 올리는 HTML `ruby` 태그로 표시한다.

### 진도 기록 (localStorage 키: `dokudo_progress_v1`)

```json
{
  "finished": {"l1-cat": true},
  "taps": {"l1-cat": {"tapped": 2, "glossTokens": 25, "keys": ["0-0", "2-3"]}},
  "wordTaps": {"ねこ": 4},
  "kana": {"pages": {"a": true, "ka": true}, "done": false},
  "currentLevel": 1,
  "lastSaved": "2026-07-28T09:00:00"
}
```

- `taps[책id].keys`: 탭한 토큰의 위치(문장번호-토큰번호). 같은 단어를 여러 번 탭해도 1개로 세기 위한 장부.
- `tapped` = keys의 개수, `glossTokens` = 그 책에서 뜻(`g`)이 있는 토큰 수. 이 둘의 비율이 i+1 엔진(6장)의 재료다.
- `wordTaps`: 단어별 누적 탭 횟수 (자주 찾아본 단어 파악용).
- `kana`: 0단계 진행 — 행별 '다 봤어요' 체크(`pages`)와 마지막 '이제 읽을 수 있어요' 체크(`done`). done이 되면 책장에서 레벨 1을 권유하는 문구가 바뀐다 (잠금은 없음).

---

## 6. i+1 엔진 규칙

- 지문 완독 시 **탭 비율 = 탭한 단어 수 ÷ 뜻(`g`) 있는 토큰 수**를 계산해 기록한다.
- 판단은 한 편 단위가 아니라 **최근 2~3편 평균**으로 한다 (한 편은 편차가 크다).

| 탭 비율 | 해석 | 앱의 행동 |
|---|---|---|
| 5% 초과 | 어려움 | 같은 레벨 유지 또는 아래 레벨 권장 |
| 2~5% | 최적 (i+1) | 유지 |
| 2% 미만 | 너무 쉬움 | 다음 레벨 책장 열어주기 |

- 추천은 **권유**로만 표시한다. 강제 이동·잠금은 하지 않는다 (재미 우선 원칙).

---

## 7. 듣기 (TTS)

- 브라우저 내장 음성 읽기 기능(Web Speech API, `speechSynthesis`)을 사용한다. 별도 음성 파일이나 유료 API가 필요 없다.
- 언어 설정은 `ja-JP`. 크롬·사파리 모두 일본어 음성이 내장되어 있다.
- 완전 초보 커리큘럼에서 TTS는 **0단계의 필수 재료**다 (글자 = 소리 연결이 학습의 절반).
- 구현됨 (2026-07-28): 0단계 글자·단어 탭 시 소리, 리더의 문장별 재생 버튼(▶).

---

## 8. 진도 동기화 (데스크탑 ↔ 폰)

여러 명이 쓰더라도 동기화는 **각자 자기 기기끼리**의 문제다 (예: 내 컴퓨터 진도를 내 폰으로). 사람 사이에 진도를 주고받는 기능은 없다.

GitHub Pages는 정적 호스팅(파일 제공만, 저장 불가)이므로 서버 저장은 불가능하다. 따라서:

| 순위 | 방식 | 비고 |
|---|---|---|
| 1차 구현 ✅ (2026-07-29) | 진도 파일 내보내기/불러오기 + 코드 복사/붙여넣기 | 책장 하단 "진도 옮기기" 버튼. 불러오면 기존 진도와 **합쳐짐** (덮어쓰지 않음 — 옛 파일을 불러와도 안전) |
| 보류 | 구글 드라이브 등 클라우드 연동 | 필요해지면 |

주의: 아이폰은 **홈 화면 앱과 사파리가 저장 공간을 따로 씀** — 같은 폰인데도 진도가 서로 안 보이는 게 정상. 이때도 진도 옮기기로 해결.

### 역할 분담 (이론과 연결)
- 데스크탑 = 새 입력(i+1). 큰 화면으로 긴 지문 읽기.
- 폰 = 반복 노출. 출퇴근길 듣기, 짧은 세션으로 같은 지문 다시 읽기.

---

## 9. 현재 상태

**구현됨 — `index.html` (2026-07-28)**
- 단일 HTML 파일. 더블클릭으로 열리고, GitHub Pages에 그대로 올릴 수 있다.
- 책장 화면: 세로쓰기 책등, 레벨별 색상, 완독 표시(✓), 처음 사용자를 글자책으로 안내
- **0단계 히라가나 글자책**: 행별 페이지 13개 (기본 46자 + 탁음 2페이지 + 섞어 읽기), 글자 탭 → 소리 + 한국어 발음 힌트, 배운 글자만 쓰는 꼬마 단어, 자가 체크 (시험·점수 없음)
- 리더: 후리가나(ruby), 단어 탭 → 하단 검은 스트립에 뜻 표시, 문장별 읽어주기 버튼(▶), "다 읽었어요" 체크
- 가타카나 처리: 지문 속 가타카나(パン·コーヒー·カレー·スーパー 등)에 히라가나 후리가나
- **진도 영구 저장 (localStorage)** — 완독 여부, 탭 기록, 단어별 탭 횟수, 글자책 진행. 새로고침·재시작해도 유지
- **지문 20편 (레벨 1 × 10, 레벨 2 × 10)** — 전부 창작 원본, 이야기 안 핵심 단어 3회+ 반복 설계, 일상 소재(아침·식사·산책·장보기·회사·주말) 중심. 책장은 레벨별 칸으로 구분, 칸 안에서 가로 스크롤
- **삽화** — 책마다 1점, 리더 상단에 표시. 직접 그린 SVG(`BOOK_ART` 맵)라 저작권 문제 없음. 디자인 팔레트 색으로 통일 (그림 = 이해 가능한 입력의 일부)
- 모바일 화면 고정: 더블탭·핀치 확대 방지(viewport, touch-action, gesturestart), 가로 밀림 방지(overflow-x hidden)
- **앱 내 새로고침·업데이트 감지** — 홈 화면 앱에는 브라우저 새로고침이 없으므로: 책장 하단 "새로고침 · v버전" 버튼(캐시 무시 재로딩) + 앱을 열거나 돌아올 때 배포본의 `APP_VERSION`을 비교해 다르면 책장 상단에 업데이트 배너 표시
- **앱 아이콘 + 매니페스트** — 옅은 빨강(#DE8F84) 바탕에 명조체 あ (`icon-512/192/180.png`, 직접 제작). `manifest.json`(display: standalone)으로 홈 화면 추가 시 앱처럼 열림
- **뒤로가기 제스처** — 화면 이동을 History API에 기록: 아이폰 왼쪽 가장자리 스와이프·안드로이드 뒤로가기로 이전 화면(책→책장, 글자책 페이지→목차→책장) 이동. 글자책 안 이전/다음 줄 이동은 기록을 쌓지 않음(replaceState)

- **진도 옮기기 (8장 1차)** — 파일 저장/코드 복사로 내보내고, 파일 선택/코드 붙여넣기로 불러오기. 불러온 진도는 기존과 합침(merge)

**미구현 (개발 대상)**
- i+1 엔진 (6장 규칙) — 탭 데이터는 이미 쌓이고 있음
- 콘텐츠 추가 확장 (레벨 3 등)

**참고 파일**: `dokudo-shelf.jsx` — 최초 React 프로토타입 (참고용으로만 보관, 더 이상 수정하지 않음)

---

## 10. 디자인 지침 (프로토타입 유지)

| 요소 | 값 |
|---|---|
| 배경 | `#F1EAD9` (종이색) |
| 본문 글자 | `#22201B` |
| 포인트 | `#B23A2E` (완독 표시 등) |
| 일본어 서체 | Zen Old Mincho |
| 한글 서체 | Noto Sans KR |
| 분위기 | 종이책 서가 느낌. 게임화 요소·점수·타이머 없음 |

---

## 11. 개발 로드맵

1. ~~`dokudo-shelf.jsx` → 단일 HTML 전환 + localStorage 진도 저장~~ ✅ 완료 (2026-07-28)
2. ~~0단계 히라가나 글자책 + TTS (글자·단어·문장 재생)~~ ✅ 완료 (2026-07-28)
3. i+1 엔진 (탭 비율 기록 → 레벨 권유)
4. ~~진도 내보내기/불러오기~~ ✅ 완료 (2026-07-29)
5. ~~콘텐츠 확장 1차 — 레벨별 10편, 핵심 단어 반복 설계~~ ✅ 완료 (2026-07-28) — 추가 확장은 피드백 후
6. ~~GitHub Pages 배포~~ ✅ 완료 (2026-07-28) — https://taehunyeoul-dot.github.io/dokudo/ · 직원 공유와 폰 홈 화면 추가 안내는 README 참고
7. (선택) 레벨 3 추가, Tatoeba 보조 예문

각 단계는 독립적으로 완결되게 진행한다 — 1번만 끝나도 실제 학습에 쓸 수 있어야 한다.
